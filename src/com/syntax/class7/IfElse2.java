package com.syntax.class7;

public class IfElse2 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		// when code executes but does not produce ex[ected results we call these mistakes logical 
		// when code does not even execute we call these mistakes syntax errors 
			
		String day = "Monday"; //in non primitive type variables we can store null which means nothing 
		//if (day.equals ("Monday")) {
			//System.out.println("weekday");
		
	 if (day.equals ("Monday") || "Tuesday".equals(day))
				"Wednesday".equals(day)
				||"Thursday".equals(day) || "Friday".equals 
			System.out.println("weekday");
	}else if ("Saturday".equals (day)|| "Sunday".equals)
		System.out.println("Weekday");
  	}else {
  		System.out.println("Not a valid day");
  			
			
		}
		}

}
